namespace UI
{
    public interface IARAnimation
    {
        /// <summary>
        /// funzione che dato in input lo step effettua l'animazione dello step relativo
        /// </summary>
        public void DoAnimation(int stepIndex);
    }
}
